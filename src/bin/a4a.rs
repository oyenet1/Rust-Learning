// Topic: Decision making with match

// Display it's true or false based on a boolean value

fn main() {
    let is_there = false;
    match is_there {
        true => println!("It's true"),
        false => println!("It's false"),
    }
}
